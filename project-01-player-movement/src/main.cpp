#include <SFML/Graphics.hpp>

int main() {
    sf::RenderWindow window(sf::VideoMode(1080, 800), "Top down player", sf::Style::Resize);
    
    window.setFramerateLimit(60);

    while (window.isOpen()) {
        
    }
}